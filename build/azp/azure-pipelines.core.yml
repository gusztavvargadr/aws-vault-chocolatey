steps:
  - checkout: self
    submodules: recursive

  - script: |
      dotnet --info

      dotnet tool restore
    displayName: Init .NET

  - script: |
      dotnet cake build.cake --target version
    displayName: Init Cake
    condition: always()
