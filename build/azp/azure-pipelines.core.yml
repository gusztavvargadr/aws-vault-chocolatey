steps:
  - checkout: self
    clean: true
    submodules: recursive

  - script: |
      dotnet --info
      dotnet tool list --global

      dotnet tool install Cake.Tool --global
    displayName: Init .NET
