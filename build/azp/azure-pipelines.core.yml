steps:
  - checkout: self
    submodules: recursive

  - script: |
      git switch $(Build.SourceBranch)
    displayName: Init Git

  - script: |
      dotnet --info
      dotnet tool list --global
    displayName: Init .NET
