steps:
  - checkout: self
    submodules: recursive

  - script: |
      git switch $(branch)
    displayName: Init Git

  - script: |
      dotnet --info
      dotnet tool list --global
    displayName: Init .NET
