steps:
  - checkout: self
    clean: true
    submodules: recursive

  - script: |
      git switch -
    displayName: Init Git

  - script: |
      dotnet --info
      dotnet tool list --global
    displayName: Init .NET
